name: Daily Brief Update

on:
  schedule:
    - cron: '0 22 * * 0-5'    # 4:00 AM BDT (UTC+6) — Sun–Fri (skip Saturday)
  workflow_dispatch:           # Also allows manual trigger from GitHub UI

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 60        # Kill the job if it hangs (Opus 4.6 is slower than Sonnet)
    permissions:
      contents: write          # Needed to commit and push

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Anthropic SDK
        run: pip install "anthropic>=0.84"

      - name: Run update script
        env:
          ANTHROPIC_API_KEY:    ${{ secrets.ANTHROPIC_API_KEY }}
          BREVO_API_KEY:        ${{ secrets.BREVO_API_KEY }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
        run: python update.py

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add the-brief.html index.html
          git diff --staged --quiet || git commit -m "Daily update $(date +%Y-%m-%d)"
          git push
