# Handoff — The Brief fixes from the 2026-07-04 ecosystem review

**Created:** 2026-07-04 (BDT) by a Fable 5 orchestrator session.
**Execute with:** Claude Opus 4.8 (`/model opus`), fresh session in THIS repo (`~/Projects/clauding-lab/the-brief`). Self-contained. Read CLAUDE.md → AGENTS.md (21 landmines) → VISION.md → AGENT_LEARNINGS.md first; Master.md before touching prose, Design.md before touching CSS.
**Provenance:** 8-dimension Opus 4.8 review of the live site (issue 156, 2026-07-04) + repo + Supabase + read-only SSH; all P0/P1 factual claims adversarially verified (verdicts noted). Nothing was modified.
**Sequencing:** EconDelta fixes come FIRST (`econdelta/docs/handoff/2026-07-04-ecosystem-review-fixes.md`) — several items below depend on upstream data being unfrozen. Items marked [after E1] should wait for the EconDelta phase; everything else is independent. **Note:** the root-level `handoff.md` (2026-05-31 F4/F7 prompts) is partially superseded — F4's daily DS30 job already exists in econdelta (`scrapers/dse_dayend.py` + timer); its outage was a DSE TLS-chain failure, fixed under E1.2 there. F7 (fiscal monthly deepening) remains valid.

## Approval gate (re-confirm before acting)
1. Hetzner writes (`adnan@135.181.43.68`, repo at `/home/adnan/the-brief`): `git pull` after merges (landmine 21), new systemd units (OnFailure alert, export job), brief.service edits. **Plus ExonVPS** (`adnan-local@103.187.23.22`, ssh alias `exonhost`): the off-box heartbeat cron in item 5b lives THERE — enumerate it in the same approval.
2. Supabase DDL/RPC additions (e.g. `get_brief_by_issue`) via **Adnan's SQL editor only** (landmine 18 mechanism).
3. Prompt-file edits (`brief/claude/prompts/*.txt`) are explicitly sign-off items (AGENTS.md out-of-scope list — its `brief/claude/*.py` wording is stale, the prompts are `.txt`; landmine 20 LOCKSTEP rule) — propose, never auto-ship, and verify a real render via the no-prod dry-run (landmine 21) before merge.
4. Ship gates by change type: **SPA changes** → branch → PR → Vercel preview → Adnan approves → merge. **Pipeline-only changes** (brief/*.py) produce no meaningful Vercel preview — their gate is the no-prod dry-run fixture render (landmine 21) reviewed by Adnan + post-publish PROD verify (landmine 17).

**Effort legend:** S = under a day; M = a few days / one PR series. Priorities: P0 broken now → P3 polish.
**"E1 done" check** (gates the [after E1] items — E1 runs as a separate econdelta session, `/Users/adnanrashid/Projects/clauding-lab/econdelta/docs/handoff/2026-07-04-ecosystem-review-fixes.md`): anon SELECT shows `dsex` max(as_of) within 2 trading days of today AND `banking_sector_crar` no longer 1.56. Until then, park those items.
**First PR housekeeping:** add `.venv/**` and `docs/**` to `globalIgnores` in `eslint.config.mjs` (currently only `.next/, out/, build/, next-env.d.ts` — lint exits 1 with 32 errors from non-source files, so the lint gate is unusable until this lands; it's listed in item 19 but must ship FIRST). Also fix project `CLAUDE.md`'s "13 numbered landmines" → 21 (stale count that undermines trust in landmine references).

---

## Phase B1 — credibility hotfixes

1. **DSEX tile reads a dead legacy series (P0, S) — VERIFIED.** `brief/builders/dse.py:19` `_SPEC` wires the tile fallback to `dse_dsex_close` (dead at 5,257 / 2026-04-21) while the chart uses live `dsex` (`brief/chart_series_fetcher.py:75`, 5,516.82 / 2026-06-11). Repoint to `dsex`. [after E1 the value is also *current*, not just consistent; ship the repoint regardless.] Landmine 17: a chart/tile re-point shows on prod only after the next 06:30 publish — verify PROD after, not just preview.
2. **Fake masthead clock (P1, S) — VERIFIED.** `app/components/Masthead.tsx:37` hardcodes `14:02 BST` next to the pulsing Live dot. Drop the time, or mirror the real implementation at `StatusBar.tsx:15-16` (inline Asia/Dhaka formatting of `_fetchedAt`) — note `_fetchedAt` lives on the `BriefPayload` root (`app/page.tsx:26`) and is NOT currently plumbed into Masthead's props, so feeding it requires passing it down. Also fix `SubscribeCTA.tsx:81/:96` "7am BST" → "06:30 BDT" (wrong timezone AND time).
3. **DS30 movers gate never checks absolute age (P1, S) — VERIFIED.** `brief/chart_series_fetcher.py:293` compares tickers-vs-index only (`today` param documented "unused"); when both feeds die together it passes — 24-day-old movers rendered as current. Add an absolute-age check vs `today`; suppress or stale-mark beyond it.
4. **Non-atomic publish serves half-briefs (P1, M) — VERIFIED.** `brief/v6_publisher.py:166-249`: DELETE → INSERT briefs row with `status='published'` (schema default, `v6_schema.py:52`) → separate per-section POSTs; a mid-loop failure leaves a served empty brief (the #118 incident, AGENT_LEARNINGS 2026-05-29 — mechanism still live). Fix: insert as `status='draft'`, flip to `published` as the LAST call; confirm `get_latest_brief` filters `status=eq.published`; add the partial-failure regression test. (Reviewer-cited test facts, verify before relying: `tests/v6/test_v6_publisher.py:87` `test_publish_brief_atomic_flow` asserts call ORDER only; the sole error test `test_publish_brief_propagates_http_error` at `:131-146` raises on EVERY call, so the first-failing call is the DELETE — the safe case. No test covers briefs-INSERT-ok + later child POST failing.)
5. **Silent publish failure — the reliability kit (P1 overall; five SEPARATE deliverables, ship as 5 small PRs/changes, not one).** Root facts, VERIFIED: no `OnFailure=` in `deploy/brief.service`; notifier only fires on exit-0 (`brief/cli.py:99`); sub-editor fail → exit 4, silent.
   - **5a (S, Hetzner):** `OnFailure=` alert unit → Discord ping on any hard failure.
   - **5b (S, ExonVPS — approval gate 1):** off-box heartbeat cron checking by ~07:30 BDT that today's `brief_date` is the latest published row (Sat excluded), ALSO watching econdelta's `freshness_sentinel` run_logs row (econdelta handoff E2.1 dead-man's-switch — one cron, two watchdogs).
   - **5c (S, Hetzner — do EARLY):** `ExecStartPre=git pull --ff-only` in brief.service (single service — safe here, unlike econdelta's 16-unit cascade); kills landmine 21 for every later PR in this handoff.
   - **5d (S, code):** notifier fail-loud: post `sent=N`, alert on `N=0` with subscribers > 0 (`cli.py:99-111` currently swallows).
   - **5e (M, Hetzner + code):** weekly export of `briefs`+children — Supabase is the ONLY copy of every issue and the LLM prose is unreproducible.
6. **Subscribe form wide open (P1, M) — VERIFIED.** `app/components/SubscribeCTA.tsx:31-35` inserts into `subscribers` from the browser with the anon key; `:80` `noValidate`; only guard is non-empty trim; no route.ts/middleware/rate-limit/honeypot anywhere. Any devtools user can poison the list Brevo mails per-publish (landmine 3). Move behind a server route (or Edge Function) with email validation + rate limit + honeypot.
7. **Sub-editor auto-pass + unwired validators (P2, S+M).** `brief/pipeline_v6.py:612-616`: malformed `SubeditorReview` → `verdict='pass'`, publishes UNREVIEWED (no test covers it). Fix: retry once, then hold/abort — never auto-pass. And `brief/claude/validators.py` (704 lines: slop blocklist, abbreviation tiers, chart-read caps) is imported by NOTHING in the publish path — wire as a deterministic post-editor gate in pipeline_v6 (run over todays_call + section prose; fail→retry or downgrade+log).

## Phase B2 — editorial contract (prompt edits = sign-off + LOCKSTEP + dry-run render)

LOCKSTEP note (landmine 20): items 8–9 add **sub-editor** rules only. That is lockstep-safe in this direction — the editor's own rules (e.g. `editor_v6.txt:200` "once or twice per brief") already forbid what the sub-editor will now catch, so nothing new is being demanded of the editor; the sub-editor is being taught to enforce the editor's existing contract. If a dry-run render shows the sub-editor over-flattening (killing the single earned vivid image), THEN touch `editor_v6.txt` in the same PR — decide with Adnan.

8. **Motif saturation.** Issue 156: "rented/financed, not earned" ×9 across 7 sections; "$0.39bn cover" ×6; "China top creditor" ×6 — editor_v6.txt:200 mandates "once or twice across the WHOLE brief"; subeditor has NO cross-section repetition check (§7 only guards flattening; §12 is a fixed token blocklist). Add a countable sub-editor rule: vivid metaphor ≤ home section + Today's Call; same numeric thesis stated outside its home section ≤ once; breach → revise.
9. **Cover-relation coherence.** Today's Call printed "trade gap clears its $3.43bn remittance" (inverted; §08 states it correctly). Sub-editor §2 addition: subject of "clears/covers" must be the inflow. Also: cover_metric.sub must not be a bare foreign metric (editor line 21 rule; today's "USD/BDT 122.85" under NET RESERVES violates it); bar promoting ≤2-session moves to "rotation/regime" claims (gold, ×4 today); FX-vs-Remittance near-duplicate sections rule; §13 bare-Tier-2 and §11 reference-value checks leak — move both into the deterministic validator gate (item 7).

## Phase B3 — surface what EconDelta already has (cheap wins) [after E1 confirms freshness]

10. **Auction calendar + results.** Supabase `auction_calendar` (forward to 2026-09-22, sizes) + `auction_results` (bid/cover/WAM/cutoff) are live, anon-readable, unread. Fills the reader-review's #1 gap: a dated "Week Ahead" block (next auctions with sizes; CPI release; MPS date) + per-print bid/cover in §06.
11. **Money-market line.** `call_money_rate` (+`_1d/7d/14d`), `crr_utilisation_pct`, `slr_utilisation_pct`, `interbank_repo_data` (NB: bank-to-bank, NOT BB repo) are live daily and unsurfaced — §02's missing overnight rate + liquidity quantification.
12. **Stop hardcoding the corridor.** `brief/builders/bb.py` pins `10.0/8.5/11.5` as constants while `policy_rate_repo/sdf/slf` are live (wait for econdelta E1.4 — SDF currently writes a wrong 7.5). Also delete the dead `bb_gross_reserves` fallback (last row 2026-03-01, n=1) in favor of live `gross_reserves_usd_bn`.
13. **Read the freshness view.** Once econdelta E3.1 ships `v_metric_freshness`, replace ad-hoc staleness checks (`brief/cadence.py`, per-fetcher gates) with the shared view; cite the versioned data contract in AGENTS.md.

## Phase B4 — distribution + trust

14. **OG/Twitter cards (P1, S) — VERIFIED zero og:/twitter: tags.** `app/layout.tsx:12-19` has title/description only. Add `generateMetadata` (per-issue title "The Brief · No. N · …"), `metadataBase`, and a dynamic OG image (`app/opengraph-image.tsx`, ImageResponse, masthead numbers in steel-crimson).
15. **Permalinks + archive (P1, M) — VERIFIED.** Briefs persist per-issue (publisher deletes only same `issue_no`); only `get_latest_brief` + routes `/` and `/preview` exist; email CTA links the bare homepage (`notifier.py:27` `_HOSTED_URL`, used at `:230` — decays to the wrong issue). Add `get_brief_by_issue` RPC (SQL editor — approval), `app/brief/[issueNo]/page.tsx`, `/archive` index, `app/sitemap.ts`. **Ordering trap:** the email-CTA change at `notifier.py:230` must ship in the same PR as (or after) the route + RPC, or every email links a 404. Landmine 18 mechanism applies to the RPC.
16. **Email deliverability + honesty (P2, S).** Add `List-Unsubscribe` + `List-Unsubscribe-Post` headers (Brevo payload, `notifier.py:363-369`); route unsubscribe off the personal Gmail; reconcile "Unsubscribe in one click" copy. Verify or soften the "2,400 readers" claim (`SubscribeCTA.tsx:59-61`) — Adnan decides the real number.
17. **Trust kit (P2, M).** `/about` route: who runs it, how it's made (AI-composed over EconDelta/BB/DSE data), sources, correction policy. Link from masthead + email footer.

## Phase B5 — design + code health batch

18. Today's Call: italic mono at ~129ch/line (`.tb-todays-call .body`, globals.css:243-249) → `max-width: 68ch`, drop italic. `--ink-4` fails WCAG as text (3.28:1 on paper; used at 9.5–14px, globals.css:358/448/473/696) → darken to ~`#676C70` or swap to `--ink-3`. Add `prefers-reduced-motion` (pulse at :177-181/:303-307, smooth-scroll :161). Mobile: headline grid stays 2-col at 375px (`:904`) → 1-col under ~560px; hero band overflow (`is-hero` margin −32px vs 20px padding, :833-837/:894); masthead meta collision at 1440px (:171-175 needs `gap`). Contract fixes: lens pill 999px radius + off-palette fallbacks (:962-971), `.tb-cta-dark` bone creams (:682/:685), `.tb-held-footer` undefined var (:821). Issue-rail §-numbering shows §07 twice vs SecNav's clean §01–06.
19. Code: eslint `globalIgnores` fix — **pulled forward to the approval-gate housekeeping above; ship it first, not here**; `lib/chartConfigs.ts` 1371 lines with ~90% duplicate builders → factories (guarded by new vitest tests: every configKey's scales registered in BriefChart — converts landmines 2/16 into enforced guards; there are ZERO frontend tests today); dynamic-import Chart.js (~60-90KB below-fold, BriefChart.tsx:16-35 eager); static fallback (issue 87 @ 2026-04-20, `lib/staticFallback.ts`) needs an unmissable banner; skip the redundant client re-fetch when server data is fresh (ClientApp.tsx:88-113); MetricV6 coercion contradicts the "strict" docstring (v6_schema.py:5-6 vs :118-126) — reject or warn-log.

## Verification discipline
Full gates, exact commands + exit codes (`npm run build && npx tsc --noEmit`, `.venv/bin/pytest -q`, lint once unblocked). Landmine 21 after every merge that must reach a scheduled brief: `git pull --ff-only` on Hetzner + confirm HEAD (until item 5c automates it). Landmine 17 for anything touching chart series. Preview URL to Adnan before every merge. Log incidents to AGENT_LEARNINGS.md (fake clock, non-atomic publish, auto-pass are rulebook-worthy).
