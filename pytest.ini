[pytest]
testpaths = tests
# Coverage threshold enforced in CI only (not here) — avoids tripping during TDD red phase
addopts = -q --cov=brief --cov-report=term-missing
markers =
    integration: end-to-end tests that shell out or hit Supabase
