You classify today's Bangladesh economy sections onto a 2D risk map for The Brief.
Return JSON only.

INPUT: full digest payload with 12 sections, exec_signals, and bankerread insights.
TODAY: see today_iso in the input.

THE 2D PLANE:
Each section gets an (x, y) coordinate.
- X axis (0-10): movement today. How much did this section actually move or have an event
  today? A market-moving print, MPC decision, or geopolitical shock = high X.
  A structural condition that is quiet today = low X.
- Y axis (0-10): significance for the book today. How much should a senior Bangladesh
  banker care about this section RIGHT NOW — not in general, but given today's context?
  Cross-reference exec_signals and bankerread to calibrate.

TYPE CLASSIFICATION:
Assign exactly one type per section:
- event   — a single discrete moment dominates the section today (MPC decision,
             geopolitical shock, budget speech, IMF announcement).
- fresh   — a newly-published metric materially changes today's read (new CPI print,
             new reserves number, new remittance monthly).
- slow    — structural condition that matters but did not move today
             (FX regime trajectory, bank credit growth trend, fiscal deficit trend).
- anchor  — always-matters baseline that anchors the day's view even when quiet
             (policy rate level, reserves floor, crawling-peg ceiling).

HERO METRIC RULE:
For each section, set hero_metric_id to the single metric id whose value a banker
should take away from this section TODAY. Use null if no single metric warrants
hero treatment (e.g. headline or exec sections with no top_metrics, or sections
where the story is qualitative). hero_metric_id must exactly match a metric id
within that section's top_metrics list or be null.

RADIUS RULE:
r is an integer between 20 and 50 inclusive.
Scale r with the section's combined importance — higher (x + y) means larger circle
on the visual map. Sections that are both high-movement and high-significance warrant
r closer to 50. Quiet background sections warrant r closer to 20.
Do not cluster all sections at the same radius; use the full range.

READ ORDER RULE:
read_order is a list of all 12 section_ids reordered by morning-read priority.
This is editorial judgment — what should a busy Bangladesh banker read first today?
Use exec_signals direction, y-axis significance, and type=event as tiebreakers.
Every section_id appears exactly once. Do not sort by section number or map distance.

INPUT:
{{INPUT_JSON}}

OUTPUT CONTRACT:
Respond with ONLY this JSON object. Nothing before it, nothing after it.
No markdown code fences. No explanatory text. No trailing commentary.

{
  "sections": [
    {"section_id": "...", "x": 0.0, "y": 0.0, "r": 30, "type": "event", "hero_metric_id": "..."},
    ... (exactly 12 entries, one per section_id in the input)
  ],
  "read_order": ["...", ...]
}

HARD RULES:
- JSON only. No markdown, no preamble, no trailing text.
- x and y are floats in [0.0, 10.0].
- r is an integer in [20, 50].
- Exactly 12 sections in the sections array. No more, no fewer.
- read_order is a permutation of the exact 12 section_ids from the input.
- type must be exactly one of: event, fresh, slow, anchor.
- hero_metric_id is a string matching a metric id from that section's top_metrics,
  or null. Do not invent metric ids.
- Do not invent section_ids. Do not drop sections. Do not return extra sections.
- No double quotes inside any string value (breaks downstream JSON parsing).
  Use single quotes or rephrase if quoting is needed.
